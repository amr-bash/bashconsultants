{
  "name": "bashconsultants",
  "version": "1.0.0",
  "private": true,
  "description": "BASH Consultants website - Jekyll static site for Azure Static Web Apps",
  "scripts": {
    "build": "bash -lc 'if [ -n \"$INPUT_AZURE_STATIC_WEB_APPS_API_TOKEN\" ]; then BUNDLE_GEMFILE=Gemfile.azure bundle install && BUNDLE_GEMFILE=Gemfile.azure bundle exec jekyll build --config _config.yml,_config.azure.yml -d _site; else bundle install && bundle exec jekyll build -d _site; fi'",
    "build:azure": "BUNDLE_GEMFILE=Gemfile.azure bundle install && BUNDLE_GEMFILE=Gemfile.azure bundle exec jekyll build --config _config.yml,_config.azure.yml -d _site"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/bamr87/bashconsultants.git"
  },
  "author": "BASH Consultants",
  "license": "MIT"
}
